# shangzi-design-system v2 · 同事使用指南

> 一句话：把你的报告 MD 交给 Claude Code，它会**先陪你把内容聊透，再一页一页做出上咨风格的 HTML deck**，全程你说了算。
> 交付物：双击即演示的 16:9 HTML deck（可浏览器内改字、保存、导出 PDF）。

---

## 1. 环境准备（一次性）

| 依赖 | 说明 |
|---|---|
| **Claude Code** | 本 skill 的运行环境（用它自己的模型，**不需要你额外配 API key**） |
| **Node.js ≥ 18** | 跑 P0 机械校验脚本（`node -v` 检查） |

## 2. 安装（一次性）

```bash
# 解压交付包后，进入 skill 目录
cd shangzi-design-system
bash install.sh
```

脚本会把 skill 装到 `~/.claude/skills/shangzi-design-system`，3 个 agent 装到 `~/.claude/agents/`。看到「安装完成」即可。

手动安装（等效）：把整个 `shangzi-design-system/` 拷到 `~/.claude/skills/`，把 `agents/` 下 3 个 `.md` 拷到 `~/.claude/agents/`。

## 3. 快速上手

1. 新建一个工作文件夹（产物会放在里面的 `working/`），把报告 MD 放进去；
2. 在该目录打开 Claude Code；
3. 说：**「帮我把 xxx.md 做成汇报 deck」**（或「做 HTML 汇报」「把 MD 转 HTML 演示」等触发词）。

然后跟着走 6 个阶段，**每个 ⭐ 门控都会停下来等你确认**，不确认不前进：

```
阶段0 入口路由    → 选路径（A 我有文档 / B 我有 dummy 草图）+ 节奏（单页迭代 推荐 / 整 Deck）
阶段1 素材接入    → 读入你的 MD，报告标题/章节统计
阶段2 内容打磨 ⭐  → 你提要求（「第三部分拆成两节」「补段结论」「数据整理成表格」），
                     AI 改完给你修改摘要，可撤销；直到你说「内容定稿」
阶段3 拆页方案 ⭐  → AI 按 ## 标题拆页，给你 manifest 表（页/标题/版式建议），你确认
阶段4 单页循环 ⭐  → 每页三道小门控：
                     P1 先向你确认这页放什么内容（一字不改）
                     P2 确认版式（AI 推荐+备选，你来选）
                     P3 先做这一页的预览，你说「标题大一点」「换 3 列」反复调，
                        调好了说「这页可以了」→ 锁定，下一页
阶段5 组装+质检 ⭐ → 所有页装进框架成整 deck，P0 机械校验全过才交付
```

**核心体验 = 「先做一页，调好了放进框架形成整体 deck」**，不用等全部生成才发现不对。

## 4. 交付物与微调

| 文件 | 用途 |
|---|---|
| `working/<报告名>-素材.md` | 内容源（阶段 2 的修改都落这里，有快照可回退） |
| `working/pages/pg-XX-preview.html` | 每页的定稿预览 |
| `working/<报告名>-deck.html` | ⭐ 最终 deck：双击打开，←/→ 翻页 |

最终 deck 里点右上 **✏️** 进入编辑模式：直接改字/加粗/高亮 → 「保存 HTML」下载新版 / 「导出 PDF」。
结构性修改（加页/换版式/改内容）→ 回到 Claude Code 继续对话迭代。

## 5. 教它你的标准（重要）

这个 skill **会学习**。两种方式把你的标准沉淀进去：

1. **对话中直接说**：
   - 「以后封面都用深蓝底」→ 记入 `learnings.md`（经验账本），下次自动遵守；
   - 「这类调整以后你们自己定，不用问我」/「这类必须先问我」→ 移动 AI 的自主边界；
   - AI 的自主范围有三区（详见 SKILL.md）：🟢 封闭集合内的匹配微调可自主；🟡 内容增删/结构变化先问你；🔴 配色/布局/原文保真永不越界。
2. **放模板素材**：把你认可的模板 HTML 放进 `library/templates/`（入库标准见 `library/README.md`），优秀成品放 `library/exemplars/`，logo 放 `library/logos/`。生成时优先用你的模板。

账本里同类经验被反复验证后，会建议你把它晋升为正式规范——你的品味会逐渐变成系统标准。

## 6. 常见问题

- **要不要 API key？** 不需要。跑在 Claude Code 里，用的是你的 Claude 订阅。
- **校验报 FAIL？** 正常现象——QA agent 会打回构建 agent 整改，全绿才交付，你不用管过程。
- **内容会被 AI 改写吗？** 不会。内容保真是铁律：标题/数据/洞察 100% 取你的原文含标点；AI 只重组版式。全文级修改也必须是你明确下过指令才动。
- **长页提案（滚动式）？** 也支持，说「做成长页」即可（走 v1 长页流程）。
- **校验脚本手动跑**：`node scripts/validate-deck.cjs working/xx-deck.html --md working/xx-素材.md`。

## 7. 出问题找谁

skill 维护：roy（上咨数字化转型）。反馈 Bug/新规范建议直接说，会进 learnings.md 或版本迭代。
