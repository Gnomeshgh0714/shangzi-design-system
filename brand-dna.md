# 上咨汇报设计系统 · 品牌 DNA

> 本文件是上咨集团 HTML 汇报/提案产物的**底层品牌规范**。所有 deck 与长页产出都必须以此为准。
> 配色源自公司 PPT 模板主题色解析，视觉骨架参考早期提案长页。
> 维护：Claude　|　最近更新：2026-08-13

---

## 🎨 品牌色（上咨蓝梯度 + 暖黄点睛）

源自公司 PPT 模板 `theme1.xml` 的 `Custom Blue Theme` 色彩方案。蓝色梯度自洽完整，直接沿用；暖黄取自早期提案长页作点睛强调。

| 角色 | 变量 | Hex | 用途 |
|---|---|---|---|
| 深蓝 | `--blue-deep` | `#0A3D8A` | 强调底色/层级最深层/标题饰条（PPT accent1） |
| 品牌蓝（主色） | `--blue` | `#0E58C4` | 主色/标题/导航选中/按钮/链接（PPT dk2/accent2，占 6 色位） |
| 中蓝 | `--blue-mid` | `#4A8EF2` | 中间过渡色/次级强调（PPT accent4） |
| 浅蓝 | `--blue-light` | `#7AB3F5` | 浅色调/装饰/图表序列（PPT accent3） |
| 背景蓝 | `--blue-bg` | `#F0F4FA` | 页面浅蓝背景区/区块底（PPT lt2） |
| 灰 | `--gray` | `#60616B` | 次要文字/分割线/标签（PPT accent5） |
| 墨（正文） | `--ink` | `#1A1A2E` | 正文主色（**非纯黑**） |
| 暗灰蓝 | `--ink-2` | `#4C4E63` | 次级正文（PPT 默认文本色） |
| 浅灰 | `--ink-3` | `#8A8A9A` | 注记/脚注/占位 |
| 暖黄（点睛） | `--yellow` | `#F4D758` | 高亮/重点/CTA/装饰线（比例 ≤10%，不做主色） |
| 暖黄浅 | `--yellow-soft` | `#FFF3CD` | 黄色高亮底纹 |
| 警示红 | `--red` | `#E84A5F` | 警示/负向对比（仅对比页/缺口页点缀） |
| 暗面板 | `--dark-panel` | `#0F2A4A` | 全宽暗色面板（蓝系深底，替代纯黑） |
| 描边 | `--line` | `#E2E8F0` | 卡片/分割线描边 |

**CSS 变量定义（两个模板共用）**：
```css
:root{
  --blue-deep:#0A3D8A; --blue:#0E58C4; --blue-mid:#4A8EF2;
  --blue-light:#7AB3F5; --blue-bg:#F0F4FA;
  --gray:#60616B; --ink:#1A1A2E; --ink-2:#4C4E63; --ink-3:#8A8A9A;
  --yellow:#F4D758; --yellow-soft:#FFF3CD; --red:#E84A5F;
  --dark-panel:#0F2A4A; --line:#E2E8F0;
  --blue-soft:rgba(14,88,196,.10);   /* 蓝色 10% 透明底 */
  --yellow-soft-bg:rgba(244,215,88,.35); /* 黄色高亮底 */
}
```

**比例原则**：蓝梯度（主色系）≥70% · 暖黄点睛 ≤10% · 灰/墨中性 20%。暖黄永远用于「强调与编码」，不做满版底色。

**蓝色梯度用法**：当主蓝不够用时，**按梯度取邻阶**（深一阶=`--blue-deep`，浅一阶=`--blue-mid`/`--blue-light`），不要发明梯度外的蓝色。

---

## 🔤 字体基因

**核心原则**：deck 走纯黑体（贴 PPT/工作 prompt 的商务汇报调）；长页走「黑体正文 + 衬线粗标题 + 英文斜体装饰」（旧长页的质感）。两场景字体在各自 scene 文件落地，brand-dna 给推荐池。

| 场景 | 用途 | 推荐字体 | 备注 |
|---|---|---|---|
| deck | 中文（标题+正文） | `"PingFang SC","Microsoft YaHei","Noto Sans SC",-apple-system,sans-serif` | 纯黑体，贴 PPT Trebuchet 的现代无衬线调 |
| deck | 西文/数字 | 同上栈回退 | 不额外引装饰字体，保持商务克制 |
| 长页 | 中文正文 | `"Noto Sans SC","PingFang SC",-apple-system,sans-serif` | 黑体正文 |
| 长页 | 中文标题 | `"Noto Serif SC"` weight 700/900 | 衬线粗标题，旧长页质感 |
| 长页 | 英文装饰/编号 | `Fraunces` italic | kicker/大编号/装饰大字 |
| 长页 | 等宽（可选） | `"Fira Code",monospace` | 代码/数据 |

**字号系统**：
- deck（1600×900 固定画布，**固定 px**）：页面标题 28/800、图表标题 22/700、观点句 19.5/700、要点 17/1.8、核心发现编号 56/800、小标题 23/800、按钮 17、脚注 11.5、图例/轴 16、数据标签 14。详见 `scene-deck.md` 字号表。
- 长页（**clamp 流式**）：Hero `clamp(2.8rem,7vw,5.5rem)`、Section 标题 `clamp(1.7rem,3.7vw,2.8rem)`、卡片标题 `1.15~1.4rem`、正文 16px、辅助 `0.78~0.85rem`、装饰大字 `clamp(8rem,22vw,20rem)` opacity 0.1~0.2。
- **铁律**：对比极端、宁大勿小（远距离审阅场景）。

---

## ✨ 气质关键词

- 冷蓝商务·简洁·信息导向
- 有质感、不廉价
- **不像 AI**（最高优先级）
- 上咨品牌感（蓝色梯度可识别）
- 结构清晰、逻辑关系用版式表达
- 暖黄点睛提气，但不喧宾夺主

---

## 🎨 配色扩展原则

当蓝梯度 + 暖黄不够用时：
- 背景永远偏冷蓝 `--blue-bg`/白，不用纯黑纯白做大面积底。
- 文字永远非纯黑 `--ink`/`--ink-2`，不用 `#000`。
- 暗色场景用 `--dark-panel`（蓝系深底），不用纯黑。
- **数据可视化色阶**：价格段/分类序列优先用蓝色梯度（深→浅）；多序列不足时按 `--blue-deep → --blue → --blue-mid → --blue-light → --gray` 取，仍不足引入暖黄系。详见 `scene-deck.md` SEGC/ATTRCOLOR/LBLC 映射表。
- **热力图**：保留红→黄→绿插值 `ryg()`（负向红 `#EF4444` → 中性黄 `#FACC15` → 正向绿 `#22C55E`），这是数据语义色，不属品牌色，仅用于热力表单元格。
- **数据标签自动配色**：`colauto()` 按色块亮度自动黑/白字（`lum<0.62` 白字，否则 `--ink`）。

---

## 🚫 通用禁忌清单

| 类型 | 禁止 |
|---|---|
| 配色 | 纯黑 `#000` 做页面背景；纯白 `#fff` 做整屏页面背景（白底 section/卡片允许）；旧参考 skill 暖三色（`#2B7FD8`/`#fefcf6`/`#faf6eb` 系）混入；发明蓝梯度外的蓝色；暖黄做满版底色 |
| 字体 | Inter/Roboto；deck 里用衬线标题（破坏商务调）；长页里全无衬线（缺质感） |
| 布局 | 居中病（整页内容挤中部留大量空白）；deck 页内出现滚动条/内容被裁；cards 嵌套 cards 超过 2 层 |
| 动效 | bounce/elastic 弹跳；neon 霓虹；AI 光效；渐变文字 |
| 装饰 | 蓝紫渐变；glassmorphism 玻璃拟态；emoji 当图标（用 SVG）；文本箭头 `->`/`▼`/`↓`（用 CSS 伪元素或 SVG） |
| 排版 | HTML 默认样式（裸 `<ul>/<table>/<blockquote>` 无 class）；字号对比不足（大的不够大、小的不够小） |
| 整体 | 截图发出去被说「又是 AI 做的」 |

**自检三问**：① 像不像上咨出的？② 蓝色梯度是否可识别？③ 有没有哪个部分「见过很多次了」？

---

## 📐 通用间距原则

- **deck**（固定 px，相对 1600×900）：`.slide` padding `10px 34px 24px`；内容区占页面 80% 高、水平居中；栏目 gap 22px；卡片 padding 12-16px；圆角 10-12px。
- **长页**（clamp 流式）：section 间 `clamp(72px,11vh,140px) 0`；容器 `max-width:1280px` + `clamp(24px,5vw,64px)` 左右 padding；卡片 padding `clamp(28px,3vw,44px)`；元素 gap `clamp(24px,3vw,48px)`。
- 装饰边框统一：黄色装饰边 `4px`，蓝色饰条 `6px`。

---

## 📱 响应式通用规则

- **deck**：不靠媒体查询，靠 JS `transform:scale` 整页等比缩放（任何分辨率版式逐像素一致）。画布内部允许相对 1600×900 的百分比布局；**禁止** vw/vh/媒体查询/百分比字号。
- **长页**：断点 `max-width:900px`（多栏降单栏、侧边导航隐藏）、`max-width:560px`（网格列数减半）。移动端是「重新排列」不是「缩小」。尊重 `prefers-reduced-motion`。

---

## 🔍 细节规范

- 选中文本高亮：`::selection{background:var(--yellow);color:var(--ink);}`
- 链接悬停：用暖黄底色块或蓝色下划线，不变色为非品牌色。
- 卡片阴影：`0 8px 40px rgba(15,23,42,.18)`（工作 prompt 验证过的 deck 阴影）。
- 圆角体系：卡片 10-12px、标签/按钮 6-20px（按语义）、装饰大色块 14-16px。
- 图标：统一用内联 SVG（单色描边，跟随当前 color），不用 emoji、不用外部图标库。

---

*本文件是上咨 HTML 产物的品牌底层。任何配色/字体/气质调整，先改这里，再同步两个模板与 scene 文件。*
