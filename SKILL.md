---
name: shangzi-design-system
description: 上咨集团 HTML 汇报/提案制作 skill（v2 · MD 工作台模式）。MD 先行：上传/粘贴报告 MD → 与 AI 多轮对话打磨内容与结构 → 内容定稿 → 按标题骨架拆页 → 逐页「确认内容→确认版式→单页试做调好」→ 装进框架成整体 deck → P0 机械校验交付。带自主边界三区表、自学习经验账本（learnings.md）与可扩展模板素材库（library/）。上咨品牌风格（蓝色梯度+暖黄点睛）16:9。触发词：做 HTML 汇报/做 deck/做 PPT/市场监测报告/洞察报告/把报告转 HTML/把 MD 转 HTML 演示/打磨报告/方案提案/战略汇报。
---

# 上咨汇报设计系统 · SKILL v2（MD 工作台模式）

> 本 skill 是上咨集团所有 HTML 汇报/提案产物的**唯一真理源**。
> 三层架构：**SKILL.md（流程）→ brand-dna.md + references/*（规范）→ assets/template-*.html（起点）**。
>
> **v2 核心理念：内容先行，逐页门控。** 先把 MD 内容打磨定稿，再拆页，然后**一页一页来**：每页动手前先向用户确认内容与版式，先做单页预览，调好了才装进框架成整体 deck。
> 设计哲学不变：限制 AI 自由度 = 保证输出一致性。不发明配色、不发明布局、不从零写、做完必自检。

---

## 开工前必读（每次任务开始时）

1. **`learnings.md`**（经验账本）——过往验证过的偏好、打回教训、边界规则。**三个 agent 同样必读**。
2. `brand-dna.md` —— 配色/字体/禁忌。
3. 场景文件：deck → `references/scene-deck.md`；长页 → `references/scene-long.md`。
4. `library/templates/` 检查有无自制模板（有则优先，见「模板素材库」一节）。

## 四条内容铁律（贯穿全流程）

1. **MD 是唯一草稿源**：所有修改先落 MD；生成阶段只从 MD 取内容，不现编。
2. **内容保真**：标题/正文/洞察/脚注/数据 100% 取素材原文含标点，禁止改写、概括、补充、翻译。
3. **只改要求处**：AI 修改 MD 时只动用户指令涉及的部分，不擅自删除、压缩没提到的内容。
4. **生成确定性**：原语集合封闭（8 个），组件取自 `references/components.md`，配色取自 `brand-dna.md`，HTML 骨架取自模板（`library/templates/` 优先，`assets/template-deck.html` 兜底）。AI 做匹配，不做创作。

## AI 自主边界 · 三区表（主观能动性在哪里，边界在哪里）

> 输入本工具的核心是**既往标准**（brand-dna/references/learnings），AI 的发挥空间被明确限定在绿区；黄区必须先问；红区永不越界。
> 边界不是拍脑袋定的——通过「不断尝试 → 记录 → 复盘」迭代出来（见 learnings.md 机制），用户随时可以说「这类以后你们自己定」或「这类以后必须问我」来移动边界，**agent 必须当场把该指示记入 learnings.md 的 `[边界]` 条目**。

| 区 | AI 可以/必须做什么 | 典型事项 |
|---|---|---|
| 🟢 **绿区 · 自主执行** | 在封闭集合内做匹配与参数微调，不必事事请示 | 原语判定树匹配、grid 列数/split 比例等参数默认值、文案转义与富文本标记（加粗/高亮）、版式相邻不撞的调整、拆页的字数归并（80/600 字规则）、错误重试与 fallback 降级 |
| 🟡 **黄区 · 先问后动** | 涉及内容增删、结构变化、对外观感知的决策，先给方案等用户拍板 | 增删章节/页、合并拆页、改写任何原文措辞、删除「疑似冗余」的内容、选择封面形式、学习条目晋升为正式规范 |
| 🔴 **红区 · 永不越界** | 无论用户是否在场都禁止 | 发明蓝梯度外的配色、发明 8 原语外的布局、从零写 HTML 不拷模板、改写/概括素材原文、跳过 P0 校验交付、用截图判断产物质量 |

---

## 触发场景与分流

用户要求做 HTML 汇报/做 deck/做 PPT/市场监测报告/洞察报告/把报告转 HTML/把 MD 转 HTML 演示/打磨报告内容并出 deck 时触发。

**先分流场景**（拿不准就问「翻页 deck 还是滚动长页？」）：

| 用户要的是 | 场景 | 走哪条流程 |
|---|---|---|
| 数据汇报/市场监测/洞察报告（有数据表、翻页演示） | **deck** | ⭐ **v2 工作流**（本文件主体） |
| 方案/提案/战略汇报（叙事型、滚动浏览、提案式） | **长页** | v1 流程：读 `references/scene-long.md` + `assets/template-long.html`，按 `archive_old/SKILL-v1.md` 的 Step 1-8 执行 |

---

## v2 工作流总览（deck 场景）

```
阶段0 入口路由（路径 + 节奏两次选择）
  ↓
阶段1 素材接入（路径A 读MD ｜ 路径B 识dummy→骨架MD）
  ↓
阶段2 内容对话打磨 ⭐门控（content-editor agent 多轮）
  ↓
阶段3 拆页方案 ⭐门控（manifest 表用户确认）
  ↓
阶段4 单页循环 ×N ⭐每页三道小门控（推荐「单页迭代」节奏）
  P1 内容确认 → P2 版式确认 → P3 单页试做（调好→锁定）
  ↓
阶段5 组装 + P0 校验 ⭐门控（qa-validator）→ 交付
```

三个专职 agent（定义在 `agents/`，安装后以 agentType 调用；**未安装时主会话亲自执行对应角色——先读对应 agents/*.md 的角色纪律再动手**）：

| Agent | 负责 | 职责一句话 |
|---|---|---|
| `shangzi-content-editor` | 阶段 2 | 按用户指令改 MD，只改要求处，原文不丢 |
| `shangzi-deck-builder` | 阶段 4 P3 / 阶段 5 | 单页预览试做 + 整 deck 组装，确定性渲染 |
| `shangzi-qa-validator` | 阶段 5 | 跑机械校验，挑毛病，不过不放行 |

调用 agent 时任务里必须给足四样：**skill 根目录绝对路径、工作文件路径、当前阶段上下文（manifest/页码/已锁定页清单）、本阶段铁律**。

**工作目录约定**：产物一律放用户 cwd 下 `working/` —— 素材 MD、`working/history/`（修改快照）、`working/pages/`（单页预览）、`working/<报告名>-deck.html`（最终 deck）。

---

### 阶段 0 · 入口路由（主会话）

**两次选择，问清楚再动手：**

1. **路径**：
   - **路径 A · 我有文档**：用户有 MD/文字素材（主路径，全流程如下）。
   - **路径 B · 我有 dummy**：用户有色块草图/手绘/参考截图 → 用 Read 读图识别结构（每色块对应的原语与内容角色：标题/图表/洞察/正文），输出识别结果（原语 + 参数 + confidence）给用户确认，然后生成**骨架 MD**（## 章节 + 各区域内容占位说明），请用户补充实际内容（或提供素材）→ 汇入阶段 2。识别不确定时明说，让用户选。
2. **节奏**：
   - **单页迭代（推荐）**：一页一页过 P1→P2→P3，做好一页锁定一页，80% 的真实场景。
   - **整 Deck 生成**：manifest 确认后一次性出全部页面，最后整体审。适合简单/赶时间的 deck；每页门控降级为「生成后整体确认」。

### 阶段 1 · 素材接入（主会话）

1. 用户提供 MD：给路径就直接读；粘贴就落盘 `working/<报告名>-素材.md`（同名先问覆盖还是新建）。
2. 用户没有 MD：对话帮他把要点整理成 MD 骨架（`# 标题` + `> 副标题` + 若干 `## 章节`），落盘后进阶段 2。
3. 报告基础信息：标题（H1）、副标题（H1 后引语/首段）、章节数、总字数；建 `working/history/`、`working/pages/`。

### 阶段 2 · 内容对话打磨 ⭐硬门控（shangzi-content-editor）

1. 用户逐条下指令（「第三部分拆成两节」「补一段结论摘要」「数据整理成表格」…），每轮：
   - agent 修改 MD 落盘；改前快照 `working/history/<报告名>-r<N>.md`（最多留 10 份）；
   - 回复「修改摘要 + 改动位置清单」，不动没要求的地方。
2. 用户也可自己改文件，改完告知即可。
3. 铁律检查：某轮修改全文缩水 >40% 且指令无删除意图 → 视为误删，回滚快照并告警。
4. **退出门控**：用户明确说「内容定稿 / 确认 / 可以拆页了」才进阶段 3。

### 阶段 3 · 拆页方案 ⭐硬门控（主会话）

1. 以 `##` 标题骨架拆页：原则上一个 `##` 一页；<80 字小章节并入邻页；>600 字大章节拆两页；总量 4-8 页。H1+副标题不占页（系统生成封面）；结尾加系统结束页。
2. 每页推荐 1 个原语（判定树见下）+ 1-2 备选。
3. 输出 **拆页 manifest 表**：页码 / 标题（取章节原文）/ 原语 / 字数 / 该页 MD 片段摘要。
4. **退出门控**：用户确认 manifest（可调页序/合并/拆分/换原语）后才进阶段 4。每页的 MD 片段 = 该页**生产源**；此后页级微调不回写总 MD。

**原语判定树**（AI 做匹配不做创作）：

| 内容特征 | 原语 |
|---|---|
| 二维数据矩阵（品牌×价位等） | `heat-table` |
| 目录/议程/框架总览 | `agenda` |
| 单一核心观点/结论 | `statement` |
| 引述/证言 | `quote` |
| 章节过渡 | `section-divider` |
| 并列 3-4 要点 / 多分组 | `grid` |
| 流程/递进 | `split-arrow` |
| 主次结构/要点+洞察（默认） | `split-block` |

### 阶段 4 · 单页循环 ⭐每页三道小门控

对 manifest 每一页（i = 1..N）依次过 P1→P2→P3；**单页迭代节奏下每道门控都要用户点头**；整 Deck 节奏下 P1/P2 可批量确认，P3 生成后整体确认。

**P1 · 内容确认（动手前先对齐）**
1. 从该页 MD 片段抽取内容块：标题 / 要点列表 / 表格 / 洞察 / 引语 / 数字亮点 / 脚注（可复用 `distribution/webapp/generator/md-utils.js` 的 `MDUtils.extractBlocks` 逻辑，也可人工抽取）。
2. 展示给用户确认：内容块清单 + 「将以这些内容入页，一字不改」。
3. 用户可在此改该页内容（改标题措辞、删要点、补洞察）——**页级修改写入该页生产源（MD 片段），不回写总 MD**，并明确告知用户这一点。
4. 用户确认 → 进 P2。

**P2 · 版式确认**
1. 按判定树给出原语推荐 + 理由 + 2 个备选（含参数：grid 列数 / split 比例等）。
2. 用户选定原语与参数 → 进 P3。

**P3 · 单页试做（先做一页，调好再入框架）**
1. 调 `shangzi-deck-builder` 的**单页预览模式**：产出 `working/pages/<pg-id>-preview.html`（模板骨架 + 仅这一张 slide，打开即所见）。模板选取顺序：`library/templates/`（用户指定或最新）→ `assets/template-deck.html` 兜底。
2. 用户打开预览，对话微调（「标题大一点」「换 3 列」「紧凑一点」「洞察框放左边」）→ 每次微调重新生成该页预览（参数化调整，内容不变；内容与样式参数记录在该页 spec 里）。
3. **用户说「这页可以了」→ 锁定该页**（预览 HTML 即定稿），进下一页。不锁定不前进。

### 阶段 5 · 组装 + P0 校验 ⭐硬门控

1. 调 `shangzi-deck-builder` 的**组装模式**：母版骨架 + 封面页 + 全部已锁定页（原样嵌入，不重渲）+ 结束页 + 注入 `assets/editable-layer.html` → `working/<报告名>-deck.html`。
2. 调 `shangzi-qa-validator` 跑机械校验：
   ```bash
   node scripts/validate-deck.cjs working/<报告名>-deck.html --md working/<报告名>-素材.md
   ```
3. **P0 全过（exit 0）才能交付**；有 ✗ → 打回定点修复 → 重跑，直到全绿。禁止「校验不过也交付」。
4. 交付话术：产物路径；双击打开即演示（←/→ 翻页）；页级微调打开 HTML 点 ✏️ 直接改字→保存/导出 PDF；结构性修改回本 skill 对应阶段迭代。
5. **复盘回写（必做）**：把本次运行学到的东西追加进 `learnings.md`——被用户纠正的偏好、被打回的教训、新探明的边界（哪类可自主/哪类要先问）。一句话一条，带日期和类别标签。没有学到东西也要回写「本次无新增」。

---

## 自学习机制（learnings.md · 经验账本）

标准不是一次写死的，是**不断尝试输入、试出「哪里要调、哪里保留」后沉淀出来的**。闭环：

```
开工读账本（不重复踩坑）→ 执行（按三区表发挥能动性）→ 复盘回写（偏好/打回/边界）
   ↑                                                        ↓
   └────── 同类条目验证 ≥3 次 → 建议晋升为正式规范（references/ 或 brand-dna.md）←──┘
```

规则：
- 每次任务**开工必读、收工必写**（主会话负责回写，agent 在回报里把学到的交回主会话）。
- 条目带日期 + 类别标签：`[偏好]` `[打回]` `[边界]` `[标准]`。
- 用户口头移动边界（「这类以后自己定 / 这类必须问我」）→ 当场记 `[边界]` 条目。
- 账本超 30 条时，主会话应整理一次：归并重复项、把 `[标准]` 候选项列给用户确认晋升。

## 模板素材库（library/）

`library/` 是可扩展资产层，详见 `library/README.md`：
- `templates/` 自制/客户模板（**生成时优先于内置母版**，入库先过 validate-deck.cjs 骨架与禁色检查）；
- `exemplars/` 优秀成品（风格参照，agent 可 Read 学结构，不得抄内容）；
- `logos/` logo/封面素材。

---

## 关键原则（v1 全部保留）

1. **从模板起步，不从零写**（母版骨架强制）。
2. **每页布局必须不同**（相邻不撞）。
3. **内容完整保留，只重组版式**（不删字、不改写）。
4. **用上咨蓝梯度 + 暖黄点睛**，不发明配色。
5. **做完必须跑机械校验**（P0 全过才交付）。
6. **永远不截图查看**（项目铁律，见 CLAUDE.md 第 6 条）——以代码/构建输出/文件解析为准；HTML 质量用 `validate-deck.cjs` 与 DOM 解析判断，绝不靠截图。

## 禁忌（详见 brand-dna.md）

核心底线：截图发出去不被说「又是 AI 做的」；蓝色梯度可识别；像上咨出的。具体禁忌：蓝紫渐变、glassmorphism、neon、bounce/elastic、AI 光效、渐变文字、emoji 当图标、文本箭头（`->`/`▼`）、HTML 默认样式、纯黑纯白大面积底、旧参考 skill 暖三色（#2B7FD8/#fefcf6/#faf6eb）混入、旧工作 prompt 蓝 #1E40AF。

---

## 文件索引

```
shangzi-design-system/
├── SKILL.md                 # 本文件（v2 流程）
├── brand-dna.md             # 品牌基因（配色/字体/气质/禁忌）
├── learnings.md             # ⭐ 经验账本（自学习载体，开工必读/收工必写）
├── README.md                # 给人看的说明
├── 使用指南-同事版.md        # 同事安装与使用指南
├── install.sh               # 一键安装（skill + agents → ~/.claude/）
├── agents/                  # 三个专职 agent 定义（装到 ~/.claude/agents/）
│   ├── shangzi-content-editor.md   # 阶段 2 内容编辑
│   ├── shangzi-deck-builder.md     # 阶段 4 P3 单页试做 + 阶段 5 组装
│   └── shangzi-qa-validator.md     # 阶段 5 P0 校验
├── library/                 # ⭐ 模板素材库（templates/exemplars/logos，详见其 README）
├── scripts/
│   └── validate-deck.cjs    # P0 机械校验脚本（零依赖）
├── package.json             # 导出脚本依赖
├── generate_pdf.cjs         # 导出 PDF/PNG（可选）
├── convert_pptx.cjs         # 单页转可编辑 PPTX（可选）
├── assets/
│   ├── template-deck.html   # deck 母版
│   ├── template-long.html   # 长页母版
│   ├── editable-layer.html  # 可编辑层
│   ├── chart-plugins.js     # Chart.js 插件
│   └── logos/               # logo 素材
├── references/
│   ├── layouts.md           # 布局库（deck 页型 + 长页 section + 映射）
│   ├── components.md        # 组件库（slug 命名，HTML 骨架）
│   ├── checklist.md         # 通用质量清单（P0/P1/P2）
│   ├── scene-deck.md        # deck 场景规范
│   └── scene-long.md        # 长页场景规范
├── distribution/            # Web 版产品（开发中，与本 skill 并行）
└── archive_old/             # v1 归档
    └── SKILL-v1.md
```

## 版本记录

- **v2.2（2026-08-20）**：加入「成长能力」三件套——AI 自主边界三区表（绿/黄/红）、learnings.md 自学习账本（开工必读/收工必写/≥3 次验证晋升规范）、library/ 模板素材库（templates 优先于内置母版）。
- **v2.1（2026-08-20）**：补齐完整交互形态——阶段 0 入口路由（路径 A/B + 单页迭代/整 Deck 节奏选择）、阶段 4 单页循环三道小门控（P1 内容确认 / P2 版式确认 / P3 单页试做调好再锁定）、「先做一页调好再装进框架」的 pages/ 预览机制。
- **v2（2026-08-20）**：升级为 MD 工作台模式——内容先行 + 三个专职 agent + validate-deck.cjs 机械校验。v1 的 8 步流归档至 `archive_old/SKILL-v1.md`（长页场景仍沿用 v1 步骤）。
- **v1（2026-08-13）**：双场景（deck + 长页）8 步工作流，统一旧参考 skill/工作 prompt 双轨规范。
